apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/yogesh-shelke-nttd/argocd-monitoring
    targetRevision: feature/argo-cd-deployment
  destination:
    server: docker-desktop
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true